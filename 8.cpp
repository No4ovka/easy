#include "easy.h"
int itc_skv(int num1)
{
    if (num1<=0)
        return -1;
    else
        return num1*num1;
}
int itc_spr(int num1,int num2)
{
    if (num1<=0 || num2<=0)
        return -1;
    else
        return num1*num2;
}
